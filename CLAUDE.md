# CLAUDE.md

`AGENTS.md` is the canonical instruction file for this repo.
Read `AGENTS.md` in the repo root first and follow it unless the current task explicitly overrides it.
Do not treat external plan files, including items in `Downloads`, as canonical repo instructions.
Preserve the current guardrails around scoped IPC/path validation, interrupted-run recovery, live shortcut behavior, and Electron sandboxing unless the task explicitly changes them.
